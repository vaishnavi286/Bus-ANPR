# Bus-ANPR
Developed a Bus Automatic Number Plate Recognition (ANPR) System using Python and computer vision techniques to automatically detect and recognize bus number plates from images or video. The system processes vehicle images, identifies the number plate region, and extracts the registration number using Optical Character Recognition (OCR).
